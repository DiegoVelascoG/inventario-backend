# Nombre de la aplicación
spring.application.name=inventario

# Conexión a MySQL (Asegúrate que el puerto sea 3306)
spring.datasource.url=jdbc:mysql://localhost:3306/inventario?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=2320081229
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA / Hibernate
# 'update' valida tu esquema y si falta algo lo agrega, pero respeta tus tablas creadas.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect